apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv0007
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    server: localhost
    path: /var/data/pv0007